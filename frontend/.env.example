# Vite는 .env / .env.local 만 읽습니다. 복사: copy .env.example .env
# 수정 후 npm run dev 재시작

# 백엔드 서버 주소 한 줄 (끝에 / 없이). API 호출·Google 로그인 둘 다 여기 기준.
# 비우면 /api·/oauth2 는 Vite가 localhost:8080 으로만 넘김.
# 예: VITE_API_BASE_URL=https://xxxx.ngrok-free.dev
VITE_API_BASE_URL=
